const concatCountInName = (name, count) => {
  return `${name} [${count}]`;
};

const countAnimalOfPerson = (person) => {
  return 0;
};

module.exports = {
  concatCountInName,
  countAnimalOfPerson,
};
